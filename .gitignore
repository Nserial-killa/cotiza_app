# Entorno
.env

# Go
api-go/tmp/
api-go/*.exe
api-go/cotiza-api

# Python
migration-python/__pycache__/
migration-python/.venv/
migration-python/*.pyc

# Datos sensibles / export de Sheets (no versionar datos reales de clientes)
migration-python/data/*.xlsx
migration-python/data/*.xlsm
migration-python/data/*.csv

# Sistema
.DS_Store

# Datos de demostración generados por scripts/seed_demo.sh — la clave
# de API es de un solo uso y el resumen cambia en cada corrida, nunca
# se versionan.
scripts/demo_api_key.txt
scripts/demo_resumen.txt
